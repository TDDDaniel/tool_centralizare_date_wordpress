const orders = document.querySelectorAll(".order-main");

orders.forEach((order) => {
    // 3. Adăugăm event-ul pe elementul individual
    order.addEventListener("click", () => {
        
    });
});
